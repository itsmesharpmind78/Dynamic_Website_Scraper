# Dynamic Website Scraper (Infinite Scroll + Selenium)

## 📌 Project Overview
This project demonstrates a **dynamic web scraper** built with **Python & Selenium** to scrape **JavaScript-rendered websites**.  
The scraper automatically scrolls through infinite content, extracts quotes, authors, and tags, and exports the data to a CSV file.

## 📌 Features
- Real browser automation using Selenium
- Handles JavaScript-rendered content
- Infinite scrolling support
- Extracts quotes, authors, and tags
- Saves data to CSV

## 📌 Skills
- Python
- Selenium WebDriver
- XPath & CSS Selectors
- Dynamic JavaScript scraping
- Python automation
- CSV Export

## 📌 How it Works
1. Opens a Chrome browser with Selenium
2. Scrolls down until all quotes are loaded
3. Extracts each quote, author, and tag using XPath
4. Saves all data to `quotes_day4.csv`

## 📌 Usage
1. Install dependencies:
pip install selenium
pip install webdriver-manager


